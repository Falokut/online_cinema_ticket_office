log_level: "debug" # supported levels: "panic", "fatal", "error", "warning" or "warn", "info", "debug", "trace"
healthcheck_port: 7001

listen:
  host: 0.0.0.0
  port: 8080
  server_mode: "BOTH"
  
db_config: 
  host: "movies_people_db"
  port: "5432"
  username: "movies_people_service"
  db_name: "movies_people_db"
  ssl_mode: "disable"

jaeger:
  service_name: "Movies_People_Service"
  address: jaeger:6831
  log_spans: true

prometheus:
  service_name: "Movies_People_Service"
  server_config:
    host: 0.0.0.0
    port: 7000


movies_people_cache: 
  network: "tcp"
  addr: "redis:6379"
  db: 0
  movies_people_ttl: 30m

